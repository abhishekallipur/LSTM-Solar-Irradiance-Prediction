name: Auto Train Model

on:
  push:
    branches: [ main ]

jobs:
  train:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v3

      - run: pip install -r requirements.txt

      - run: python train.py
